import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'AI Assistant', description: 'AI-powered guideline development workflows — evidence synthesis, GRADE appraisal, and recommendation drafting.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
