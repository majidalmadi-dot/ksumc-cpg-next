import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Guidelines Registry', description: 'Browse, filter, and manage all clinical practice guidelines across pathways and statuses.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
