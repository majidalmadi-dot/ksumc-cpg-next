import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Reports', description: 'Generate PRISMA flow diagrams, GRADE evidence profiles, and full guideline publication documents.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
