import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Framework Compliance', description: 'AGREE II, IOM, and WHO framework compliance tracking with radar visualization.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
