import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Systematic Review', description: 'PRISMA 2020 systematic review workflow — search strategy, screening, data extraction, and meta-analysis.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
