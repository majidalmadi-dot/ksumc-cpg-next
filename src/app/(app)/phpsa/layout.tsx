import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'PHPSA', description: 'Public Healthcare Policy Society of Saudi Arabia — policy briefs, governance documents, and research outputs.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
