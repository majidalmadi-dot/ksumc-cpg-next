import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Health Technology Assessment', description: 'EUnetHTA Core Model multi-domain health technology appraisal with structured deliberation and recommendation framework.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
