import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Cost-Effectiveness Analysis', description: 'Health economic evaluation with ICER calculation, Markov modeling, and sensitivity analysis.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
