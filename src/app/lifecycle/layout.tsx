import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Lifecycle Tracker', description: 'Gantt timeline view of guideline development milestones and phase progression.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
