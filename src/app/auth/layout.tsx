import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Sign In', description: 'Secure authentication for KSUMC Clinical Practice Guideline Development Platform.' }
export default function L({ children }: { children: React.ReactNode }) { return children }
